# Python
__pycache__/
*.pyc
*.pyo
.env

# Data (Railway volume — no subir al repo)
/data/
*.log
*.csv

# No subir el modelo al repo (subirlo manualmente a Railway o via volume)
# Si lo querés incluir en el repo, comenta la siguiente línea:
# modelo.pkl
